package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Clean Minimalism Color Palette
val PrimaryEmerald = Color(0xFF0061A4)      // Rich Brand Blue
val PrimaryEmeraldDark = Color(0xFF00497E)  // Dark Blue
val SoftEmerald = Color(0xFFD1E4FF)         // Light Blue Accent (selected states/container)
val AccentTeal = Color(0xFF0061A4)          // Clean Accent Blue

// Light Theme-first Canvas colors mapped to original names for backward compatibility
val DeepNavyBg = Color(0xFFF7F9FC)          // Airy grey-blue canvas background
val CardSlate = Color(0xFFFFFFFF)           // Pristine flat white cards
val LightText = Color(0xFF1A1C1E)           // High contrast Dark Charcoal main text
val GreyText = Color(0xFF74777F)            // Soft slate gray auxiliary text

// Alert / Semantic colors
val ErrorCrimson = Color(0xFFBA1A1A)        // Refined Minimal Dark Red
val WarningAmber = Color(0xFFB06000)        // Clean Amber
val InfoSky = Color(0xFF0061A4)             // Info Brand Blue
val VerifiedGreen = Color(0xFF15803D)        // Safe Forest Green (Success)


